{
  "name": "@aws-prototype/data-source-manifest",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@types/archiver": "^5.1.1",
    "archiver": "^5.3.0",
    "aws-sdk": "^2.1664.0"
  },
  "scripts": {
    "build": "esbuild --bundle src/index.ts --target=node18 --platform=node --outfile=dist/index.js --external:aws-sdk",
    "lint": "eslint --max-warnings 0 .",
    "lint:fix": "eslint --fix ."
  }
}
